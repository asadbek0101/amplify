export enum PageType{
    Auth = "/",
    App = "/app",
}

export enum ContainerPageType{
    Account = "/app/accounts",
    Status = "/app/status",
    Branch = "/app/branch",
    Plan = "/app/plan"
}